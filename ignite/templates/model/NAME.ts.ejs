import { types } from "mobx-state-tree"

// eslint-disable-line @typescript-eslint/no-unused-vars

export const <%= props.pascalCaseName %> = types
  .model("<%= props.pascalCaseName %>")
  .props({})
  .views((self) => ({}))
  .actions((self) => ({}))

export const <%= props.pascalCaseName %>InitialState = {
  // Hier Initial-State einsetzen
}